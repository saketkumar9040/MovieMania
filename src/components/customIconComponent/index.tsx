import { View, Text } from 'react-native';
import React from 'react';

import styles from './style';

const CustomIconComponent = () => {
  return (
    <View>
      <Text>CustomIconComponent</Text>
    </View>
  )
}

export default CustomIconComponent;